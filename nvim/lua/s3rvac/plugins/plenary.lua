-- Common Neovim functions.
-- https://github.com/nvim-lua/plenary.nvim
--
-- Currently used as a dependency of CopilotChat.nvim (I have it configured as
-- a standalone plugin so that I can have the same lazy.json config across
-- machines).
return {
  "nvim-lua/plenary.nvim",
  commit = "74b06c6c75e4eeb3108ec01852001636d85a932b", -- 2026-04-10
}
